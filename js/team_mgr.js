/* 
 * runs *after* document is completely loaded
 */
window.onload = function() {
    //alert("welcome");
};

/*
 * runs *before* document is completely loaded
 */
$(document).ready(function() {
    // add event handler to all links
    //$("a").click(function( event ) {
    //    alert("Thanks for visiting!");
    //});
});
